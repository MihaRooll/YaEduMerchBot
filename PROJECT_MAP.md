# Карта проекта YaEduMerchBot

## ���️ Архитектура проекта

### Структура файлов
```
YaEduMerchBot_new/
├── data/                    # JSON файлы для хранения данных
│   ├── users.json          # Пользователи и их роли
│   ├── chats.json          # Чаты и их настройки
│   ├── inventory.json      # Товары и остатки
│   ├── orders.json         # Заказы
│   └── settings.json       # Настройки системы
├── src/                     # Исходный код
│   ├── __init__.py
│   ├── bot.py              # Основной класс бота
│   ├── storage.py          # Работа с JSON файлами
│   ├── auth.py             # Система авторизации и ролей
│   └── handlers/           # Обработчики команд
│       ├── __init__.py
│       └── admin.py        # Админские команды
├── config.py               # Конфигурация
├── main.py                 # Точка входа
├── init_data.py            # Инициализация данных
└── requirements.txt        # Зависимости
```

## ��� Система ролей

### Админ (admin)
- **Может**: ВСЕ
- **Добавляет**: админов, координаторов, промо
- **Управляет**: всеми настройками системы

### Координатор (coordinator)
- **Может**: добавлять в промо
- **Управляет**: заказами, инвентарем
- **Доступ**: ограниченный админ

### Промо (promo)
- **Может**: работать с заказами
- **Доступ**: общий чат + добавленные координатором

### Пользователь (user)
- **Может**: создавать заказы
- **Ограничения**: по лимитам

## ��� Структура данных JSON

### users.json
```json
{
  "user_id": {
    "username": "string",
    "first_name": "string",
    "role": "admin|coordinator|promo|user",
    "added_by": "user_id",
    "added_at": "timestamp",
    "is_active": true
  }
}
```

### chats.json
```json
{
  "chat_id": {
    "title": "string",
    "prefix": "string",
    "is_common": true,
    "is_active": true
  }
}
```

## ��� Этапы разработки

1. **✅ Этап 1**: Базовая структура + JSON хранилище
2. **✅ Этап 2**: Система ролей и авторизация
3. **✅ Этап 3**: Админская панель
4. **⏳ Этап 4**: Команды координатора
5. **⏳ Этап 5**: Система заказов
6. **⏳ Этап 6**: Тестирование

## ��� Технические детали

- **Хранилище**: JSON файлы (простота, портативность)
- **Бот**: pyTelegramBotAPI
- **Роли**: Иерархическая система с наследованием прав
- **Безопасность**: Проверка ролей на каждом уровне

## ��� Что уже реализовано

- ✅ Система хранения данных через JSON
- ✅ Иерархия ролей (admin > coordinator > promo > user)
- ✅ Автоматическое создание главного админа
- ✅ Админские команды (/admin, /adduser, /users, /stats)
- ✅ Проверка прав доступа
- ✅ Инициализация тестовых данных
- ✅ Базовая структура бота

## ��� Следующие шаги

1. Реализовать команды координатора
2. Добавить систему заказов
3. Реализовать FSM для создания заказов
4. Добавить операторские функции
5. Покрыть код тестами
